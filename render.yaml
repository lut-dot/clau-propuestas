services:
  - type: web
    name: clau-propuestas
    env: python
    buildCommand: apt-get install -y fonts-dejavu-core && pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
